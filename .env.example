# Copy to .env and edit if needed
# PORT the server will listen on (default 3000)
PORT=3000
# Comma-separated list of symbols to subscribe to (Bybit contract symbol format), e.g. BTCUSDT,ETHUSDT
PAIRS=BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,XRPUSDT
# Comma-separated intervals to watch in minutes (1 and/or 5)
INTERVALS=1,5
# If you deploy behind HTTPS, set FORCE_WSS=true to make frontend use wss:// instead of ws://
FORCE_WSS=false
